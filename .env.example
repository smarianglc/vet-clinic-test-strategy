# Credenciais do banco de desenvolvimento (Postgres via Docker).
# Copie este arquivo para .env e ajuste se quiser. O .env NÃO vai pro Git.
POSTGRES_USER=vetclinic
POSTGRES_PASSWORD=vetclinic_dev_pw
POSTGRES_DB=vetclinic
POSTGRES_PORT=5432
